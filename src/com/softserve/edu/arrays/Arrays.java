/*package com.softserve.edu.arrays;

public class Arrays {
    public static void main(String[] args) {
        int[] array = {2, 3,5,-10,100,-150,50,-45};
        for (int i = 0; i < array.length; i++) {
            System.out.print(array[i] + "  ");
        }
        System.out.println();
        int max = array[0];
        int imax = 0;
        int i = 0;
        while (i < array.length) {
            if (array[i] > max) {
                max = array[i];
                imax = i;
            }
            i++;
        }
        System.out.println("max: " + max);
        int sum = 0;
        for (i = 0; i < array.length; i++) {
            sum = sum + array[i];
        }
        System.out.println("sum: " + sum);
        int sumpositive = 0;
        for (i = 0; i < array.length; i++){
                if (array[i] > 0)
                    sumpositive = sumpositive + array[i];}
                System.out.println("sum positive: " + sumpositive);
        int amount = 0;
        for (int a : array){
            if (a <0 && a > -1000){amount++;}}
        System.out.println("negative amount: " + amount);





            }
    }

*/